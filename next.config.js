/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/meu_cv',
  /* config options here */
};

module.exports = nextConfig;